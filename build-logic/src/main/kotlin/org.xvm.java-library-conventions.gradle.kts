plugins {
    id("org.xvm.java-common-conventions") // Apply the common convention plugin for shared build configuration between library and application projects.
    `java-library` // Apply the java-library plugin for API and implementation separation.
}
